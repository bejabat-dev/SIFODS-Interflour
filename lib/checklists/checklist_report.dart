import 'package:flutter/material.dart';

class ChecklistReport extends StatelessWidget {
  const ChecklistReport({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}